# backgrounds

Two wallpapers ship with the theme:

- `frankenstein-castle.jpg` — gothic castle in a storm
- `frankenstein-classic.webp` — minimal teal Frankenstein on dark

Both are installed automatically to `~/.config/hypr/wallpapers/` by `install.sh`.
